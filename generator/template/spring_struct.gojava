package {{.Package}};

import java.util.List;

public class {{.ClassName}} {
    {{- range .Fields}}
    private final {{.JavaType}} {{ .Name }};
    {{- end }}

    public {{.ClassName}}({{.ContructParam}}) {
    {{- range .Fields}}
        this.{{ .Name }} = {{ .Name }};
    {{- end }}
    }

    {{range .Fields -}}
    public {{.JavaType}} get{{ .Title }}() {
        return {{ .Name }};
    }
    {{ end }}
}
